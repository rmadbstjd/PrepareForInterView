props는 부모 컴포넌트로부터 자식 컴포넌트로 전달되는 데이터입니다.

부모 컴포넌트에서 자식 컴포넌트로만 전달가능하기 때문에 단뱡향의 흐름을 갖습니다.

자식 컴포넌트에서는 props를 변경할 수 없는 읽기 전용의 속성을 갖고 있습니다.

state는 컴포넌트의 상태를 나타내며, 컴포넌트 내부에서 선언되기 때문에 변경이 가능합니다.

또한 state가 변경되면 컴포넌트는 렌더링을 다시 해야합니다.