/**
 * *map() 함수**는 원본 배열을 변형하지 않고, 새로운 배열을 반환합니다.  

주로 기존 배열을 기반으로 한 반환된 새로운 배열을 갖고 싶을 때 사용합니다.

forEach() 함수는 반환 값이 없습니다.  forEach 함수는 콜백함수를 매개 변수로 받고,

콜백 함수의 매개 변수로는 현재 순회중인 배열 요소의 값, 인덱스, forEach() 함수를 호출한 배열이 있습니다.

array.forEach(function(currentValue, index, array) {
// 콜백 함수 내에서 수행할 작업
});

주로 원본 배열을 변경하지 않고 각 요소에 대한 동작을 수행할 때 사용됩니다.*/
