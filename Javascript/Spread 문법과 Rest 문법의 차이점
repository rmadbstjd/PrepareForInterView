/\*

Spread 연산자 Rest 파라미터는 자바스크립트에서 객체와 배열을 다룰 때 사용되는 문법입니다.

이 두가지 문법은 서로 유사해보일 수 있지만, 사용되는 위치와 동작 방식에서 차이가 있습니다.

**Spread 연사자은**

주로 객체나 배열을 복사할때 사용하거나 객체나 배열 안에 또다른 객체나 배열을 합칠 때 사용됩니다.

기존의 원본 객체나 배열을 변형시키지 않고 기존과 다른 참조 값을 가진 배열을 생성합니다.

**Rest 문법은**

1. 구조 분해 할당 문법이랑 같이 사용하면 나머지(객체, 배열를 rest에 담아서 추출할 수 있습니다.

   const numArr = [1,2,3,4,5];

   const [one, two, …rest] = numArr;

   console.log(one) // 1;

   console.log(rest) // [3,4,5]

2. 함수에서 정해지지 않은 수의 매개변수를 배열로 받을 수 있습니다.

   function sum(…rest) {

   return rest;

   }

   const result = sum(1,2,3);

   console.log(result) // [1,2,3]\*/
